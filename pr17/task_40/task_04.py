# Завдання 04 — Видалення елементів з множини (remove, discard, pop)

s = {1, 2, 3, 4, 5}
s.remove(3)       # помилка якщо немає
s.discard(10)     # без помилки якщо немає
popped = s.pop()
print("Результат:", s, "| pop видалив:", popped)
