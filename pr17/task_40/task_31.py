# Завдання 31 — Видалення дублікатів зі списку через множину

lst = [3, 1, 4, 1, 5, 9, 2, 6, 5, 3]
no_dups = list(dict.fromkeys(lst))  # зберігає порядок
print("Оригінал:", lst)
print("Без дублікатів (зі збереженням порядку):", no_dups)
