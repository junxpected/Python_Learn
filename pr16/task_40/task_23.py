# Завдання 23 — Список включення з рядками (upper/lower)

words = ["python", "list", "tuple", "comprehension"]
upper_words = [w.upper() for w in words]
cap_words = [w.capitalize() for w in words]
print("Uppercase:", upper_words)
print("Capitalize:", cap_words)
